{
  "branches": "main",
  "debug": false,
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    [
      "@semantic-release/exec",
      {
        "prepareCmd": "mvn -B clean versions:set -DnewVersion=${nextRelease.version}",
        "publishCmd": "mvn -B package -DskipTests && mvn -B package -Pnative -DskipTests && cd target/quarkus-app && tar -zcvf app.tar.gz ."
      }
    ],
    [
      "@semantic-release/github",
      {
        "assets": ["target/*-runner", "app.tar.gz"]
      }
    ],
    [
      "@semantic-release/git",
      {
        "assets": ["pom.xml"],
        "message": "chore(release): ${nextRelease.version} [skip ci]\n\n${nextRelease.notes}"
      }
    ]
  ]
}